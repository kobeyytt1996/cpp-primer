#include <iostream>
#include <string>

using std::string; using std::cout;
using std::cin; using std::endl;

int main(){
    string mystring;
    getline(cin, mystring);

    for (auto &c : mystring)
        c = 'X';
    cout << mystring << endl;

    return 0;
}
