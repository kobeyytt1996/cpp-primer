#include <iostream>
#include <vector>
#include <string>

using std::string; using std::cout;
using std::cin; using std::endl;
using std::vector;

int main(){
    vector<int> i1(10,3);

    for ( auto it = i1.begin(); it != i1.end(); ++it )
    {
        *it = 2*(*it);
        cout << *it << " ";
    }
    cout << endl;

    return 0;
}
