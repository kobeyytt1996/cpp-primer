#include <iostream>
#include <vector>

using std::string; using std::cout;
using std::cin; using std::endl;
using std::vector;

int main(){
    vector<int> v1;
    int i, j = 0;
    while (cin >> i)
    {
        v1.push_back(i);
        cout << v1[j++] <<endl;
    }

    return 0;
}
