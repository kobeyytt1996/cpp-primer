#include <iostream>
#include <string>
using namespace std;

int main() {
	string grade;
	int i = 45;
	grade = (i > 90) ? "high" : (i > 75) ? "low" : (i > 60) ? "pass" : "fail";
	cout << grade << endl;
		
}



